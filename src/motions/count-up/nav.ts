import type { MotionNavConfig } from "../types";

const navConfig: MotionNavConfig = {
  id: "count-up",
  name: "数字滚动",
  category: "内容编排",
  scene: "数据看板、交易金额、关键指标展示",
};

export default navConfig;

